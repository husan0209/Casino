import { Module } from '@nestjs/common'
// BullMQ queues – to be enabled in Part 6
// queues: email, notifications, exchange-rates, expire-payments, referral-rewards
@Module({})
export class QueuesModule {}
