export enum UserStatus { active='active', blocked='blocked', suspended='suspended' }
export enum UserRole { user='user', admin='admin', superadmin='superadmin' }
export enum KycStatus { not_started='not_started', pending='pending', approved='approved', rejected='rejected', requires_resubmission='requires_resubmission' }
